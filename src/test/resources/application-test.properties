# Server Port for test (random port)
server.port=0

# Security Properties mapping directly to Environment Variables (loaded from .env)
security.admin.username=${SECURITY_ADMIN_USERNAME}
security.admin.password=${SECURITY_ADMIN_PASSWORD}

# Database Configuration (Default properties targeting the test profiles)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Application Database Schemas & Default Dataset Configuration
app.db.catalog-schema=${DB_CATALOG_SCHEMA:catalog_owner}
app.db.report-builder-schema=${DB_REPORT_BUILDER_SCHEMA:report_builder_owner}
app.db.analytics-schema=${DB_ANALYTICS_SCHEMA:${BIGQUERY_DATASET:analytics}}
app.db.date-table=${DB_DATE_TABLE:dim_date}
app.db.date-column=${DB_DATE_COLUMN:date_key}

# Logging configuration during testing
logging.level.com.reporting=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
