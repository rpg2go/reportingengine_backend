# Server Port for test (random port)
server.port=0

# Spring Security Admin environment fallback override for testing
SECURITY_ADMIN_USERNAME=admin
SECURITY_ADMIN_PASSWORD=password

# Database Configuration (Default properties targeting the test profiles)
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/agentic_ai
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging configuration during testing
logging.level.com.reporting=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
